name: Generate Games Index

on:
  workflow_dispatch:
  push:
    paths:
      - 'configs/**'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Generate JSON Index
        run: |
          python - <<EOF_PY
          import os
          import json

          configs_dir = 'configs'
          index = {}
          games_list = []

          if os.path.exists(configs_dir):
              for game_id in sorted(os.listdir(configs_dir)):
                  game_path = os.path.join(configs_dir, game_id)
                  if os.path.isdir(game_path):
                      files = [f for f in os.listdir(game_path) if f.endswith('.json')]
                      if not files: continue

                      display_name = game_id.replace('_', ' ').title()
                      steam_id = None
                      community_image = None
                      files_info = []
                      
                      # Deep scan each config to build a rich index
                      for filename in files:
                          try:
                              with open(os.path.join(game_path, filename), 'r', encoding='utf-8') as f:
                                  data = json.load(f)
                                  meta = data.get('meta', {})
                                  
                                  # Update global game info if not already set
                                  display_name = meta.get('game_name', display_name)
                                  if not steam_id: steam_id = meta.get('steam_id', None)
                                  if not community_image: community_image = meta.get('community_image', None)
                                  
                                  # Extract hardware & user metadata
                                  device = meta.get('device', {})
                                  mfr = str(device.get('manufacturer', '')).capitalize()
                                  model = str(device.get('model', ''))
                                  gpu = device.get('gpu', 'Unknown GPU')
                                  notes = meta.get('notes', '') # <--- Extracted Custom Note
                                  
                                  files_info.append({
                                      "filename": filename,
                                      "device_model": f"{mfr} {model}".strip() or "Unknown Device",
                                      "gpu": gpu,
                                      "notes": notes, # <--- Included in index
                                      "timestamp": meta.get('timestamp', 0)
                                  })
                          except Exception as e:
                              # Fallback for corrupted or old-schema files
                              files_info.append({"filename": filename, "error": str(e)})

                      games_list.append({
                          "id": game_id,
                          "name": display_name,
                          "steam_id": steam_id,
                          "community_image": community_image,
                          "config_count": len(files_info)
                      })
                      
                      # Sort configs inside each game: newest uploads first
                      index[game_id] = sorted(files_info, key=lambda x: x.get('timestamp', 0), reverse=True)

          # Output structured rich index for the App
          # index.json contains the detailed hardware/notes info for each game
          with open('index.json', 'w', encoding='utf-8') as f:
              json.dump(index, f, indent=2)

          # games.json is the lightweight "Gallery" list
          with open('games.json', 'w', encoding='utf-8') as f:
              json.dump(sorted(games_list, key=lambda x: x['name']), f, indent=2)
          EOF_PY

      - name: Sync and Push
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          
          git add games.json index.json
          
          if git diff --staged --quiet; then
            echo "No changes needed."
            exit 0
          fi
          
          git commit -m "Auto-update Index with Notes [skip ci]"
          
          # Retry loop to beat race conditions during simultaneous uploads
          for i in {1..5}; do
            echo "Attempt $i to push index..."
            if git pull --rebase origin main && git push origin main; then
              echo "Push successful."
              exit 0
            fi
            sleep 5
          done
          exit 1
